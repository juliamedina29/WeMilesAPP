

def abs_value (delta) :
    """Returns absolute value of a number"""
    abs_delta = abs(delta)
    return(abs_delta)

def offset (delta, miles) :
    """Returns delta multiplied by miles"""
    total = delta * miles
    return total

